First React Calculator 
// Build Command
expo init calculator-react      
npx expo-cli upgrade  
npm install --global @expo-cli  
npx expo install react-native-web react-dom @expo/metro-runtime

// Run Command
npx expo start
